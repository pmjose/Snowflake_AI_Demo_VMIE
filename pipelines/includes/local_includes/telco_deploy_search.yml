Telco Deploy Search Services:
  extends:
    - .agent_tag
  stage: "Telco AI Services"
  needs:
    - job: "Initialise Pipeline"
    - job: "Telco Data Foundation"
  rules:
    - if: $EVENT_DEPLOY_TELCO == "true" && $TELCO_ENABLE_SEARCH_SERVICES == "true"
  image: $DATAOPS_SNOWFLAKE_RUNNER_IMAGE
  variables:
    DATAOPS_SNOWSQL_USER: $DATAOPS_SNOWFLAKE_USER
    DATAOPS_SNOWSQL_ACCOUNT: $DATAOPS_SNOWFLAKE_ACCOUNT
    DATAOPS_SNOWSQL_ROLE: ACCOUNTADMIN
    DATAOPS_SNOWSQL_WAREHOUSE: $EVENT_WAREHOUSE
    DATAOPS_SNOWFLAKE_AUTH: KEY_PAIR
    DATAOPS_RUN_SQL: $CI_PROJECT_DIR/dataops/event/telco_deploy_search.sql
  script:
    - /dataops
  icon: ${SNOWFLAKE_ICON}

